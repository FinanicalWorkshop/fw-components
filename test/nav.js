import assert from 'assert'
import Nav from '../react/nav.js'

describe('test', function () {
    describe('', function () {
        before(function () {

        })
        it('should has a component named Nav', function () {
            assert(!!Nav, true)
        })
    })
})