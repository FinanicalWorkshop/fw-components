import assert from 'assert'
import CircleLoading from '../react/circle-loading.js'

describe('test', function () {
    describe('', function () {
        before(function () {

        })
        it('should has a component named CircleLoading', function () {
            assert(!!CircleLoading, true)
        })
    })
})