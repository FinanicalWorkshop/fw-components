import assert from 'assert'
import SVGCircleProgress from '../react/circle-progress.js'

describe('test', function () {
    describe('', function () {
        before(function () {

        })
        it('should has a component named SVGCircleProgress', function () {
            assert(!!SVGCircleProgress, true)
        })
    })
})