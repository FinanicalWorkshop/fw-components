import assert from 'assert'
import Alert from '../react/alert.js'

describe('test', function () {
    describe('', function () {
        before(function () {

        })
        it('should has a component named Alert', function () {
            assert(!!Alert, true)
        })
    })
})